{
  "type": "object",
  "properties": {
    "input_dir": { "type": "string" },
    "output_dir": { "type": "string" },
    "num_augmented_per_image": { "type": "integer", "minimum": 1 },
    "copy_original": { "type": "boolean" },
    "valid_extensions": {
      "type": "array",
      "items": { "type": "string", "pattern": "^\\.[a-zA-Z0-9]+$" }
    },
    "debug": { "type": "boolean" },
    "overwrite": { "type": "boolean" }
  },
  "required": ["input_dir", "output_dir", "num_augmented_per_image", "copy_original", "valid_extensions", "debug"],
  "additionalProperties": false
}
