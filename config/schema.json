{
  "type": "object",
  "properties": {
    "input": {
      "type": "object",
      "properties": {
        "dataset_folder": { "type": "string" }
      },
      "required": ["dataset_folder"],
      "additionalProperties": false
    },
    "output": {
      "type": "object",
      "properties": {
        "model_save_dir": { "type": "string" }
      },
      "required": ["model_save_dir"],
      "additionalProperties": false
    },
    "dataset_parameters": {
    "type": "object",
    "properties": {
        "min_samples_per_class": { "type": "integer", "minimum": 1 }
    },
    "required": ["min_samples_per_class"],
    "additionalProperties": false
    },
    "parameters": {
      "type": "object",
      "properties": {
        "train": { "type": "boolean" },
        "test": { "type": "boolean" },
        "show_preview": { "type": "boolean" },
        "debug": { "type": "boolean" },
        "load_model": { "type": "boolean" },
        "model_load_path": { "type": "string" }
      },
      "required": ["train", "test", "show_preview", "debug", "load_model", "model_load_path"],
      "additionalProperties": false
    },
    "hyper_parameters": {
      "type": "object",
      "properties": {
        "batch_size": { "type": "integer" },
        "weight_decay": { "type": "number" },
        "learning_rate": { "type": "number" },
        "momentum": { "type": "number" },
        "epochs": { "type": "integer" },
        "seed": { "type": "integer" }
      },
      "required": ["batch_size", "weight_decay", "learning_rate", "momentum", "epochs", "seed"],
      "additionalProperties": false
    },
    "train_parameters": {
      "type": "object",
      "properties": {
        "network_type": { "type": "string" },
        "train_split": { "type": "number", "minimum": 0, "maximum": 1 },
        "val_split": { "type": "number", "minimum": 0, "maximum": 1 },
        "test_split": { "type": "number", "minimum": 0, "maximum": 1 },
        "accuracy_evaluation_epochs": { "type": "integer", "minimum": 1 },
        "accuracy_target": { "type": "number", "minimum": 0, "maximum": 100 }
      },
      "required": ["network_type", "train_split", "val_split", "test_split", "accuracy_evaluation_epochs", "accuracy_target"],
      "additionalProperties": false
    },
    "early_stop_parameters": {
      "type": "object",
      "properties": {
        "start_epoch": { "type": "integer", "minimum": 0 },
        "loss_evaluation_epochs": { "type": "integer", "minimum": 1 },
        "patience": { "type": "integer", "minimum": 1 },
        "improvement_rate": { "type": "number", "minimum": 0 }
      },
      "required": ["start_epoch", "loss_evaluation_epochs", "patience", "improvement_rate"],
      "additionalProperties": false
    }
  },
  "required": ["input", "output", "dataset_parameters", "parameters", "hyper_parameters", "train_parameters", "early_stop_parameters"],
  "additionalProperties": true
}
